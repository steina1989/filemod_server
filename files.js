let file_list = [];
let file_tree = [];

/**
 * Reads a file's content, adds it's content to file_list.
 * Handles new files and modified files
 * 
 * @param {string} path - path to file
 * @returns {object} - diff patch
 */
async function fileChange(path) {

}

async function addDirectory(path) {

}


module.exports.filemanager = {
    addFile,

}